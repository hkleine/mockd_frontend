import React from 'react';

const DashboardContent = ({ children }) => (
  <main class="flex-1 w-full h-full overflow-x-hidden overflow-y-auto bg-gray-100 bg-opacity-75">{children}</main>
);

export default DashboardContent;
