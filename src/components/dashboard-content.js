import React from 'react';

const DashboardContent = ({ children }) => (
  <main className="flex-1 w-full h-full overflow-x-hidden overflow-y-auto bg-gray-200 bg-opacity-75 pt-12 px-16">{children}</main>
);

export default DashboardContent;
