import React from "react";

const DashboardContent = ({children}) => (

<main class="flex-1 w-full h-full overflow-x-hidden overflow-y-auto bg-white">
    {children}
</main>
);

export default DashboardContent;
